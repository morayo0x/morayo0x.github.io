#!/bin/sh
# .husky/pre-commit
# Runs before every commit. Keeps the repo clean.
# Setup: npm install husky lint-staged --save-dev && npx husky init

. "$(dirname "$0")/_/husky.sh"

# Run lint-staged (only checks staged files — fast)
npx lint-staged
